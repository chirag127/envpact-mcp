{
  "name": "envpact-mcp-worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker variant of envpact-mcp — fetches the user's private envpact-secrets vault via the GitHub Contents API and serves MCP over Streamable HTTP at https://mcp.envpact.oriz.in/mcp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  },
  "packageManager": "pnpm@10.18.0"
}
